
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Безопасность будет добавлена здесь
    match /{document=**} {
      allow read, write: if true; // Временно открыто для разработки
    }
  }
}
